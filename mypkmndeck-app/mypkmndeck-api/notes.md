# Notes

Due to not being able to host this API for free with a data limit etc. I have had to include the API project as a local "project" so it just uses memory instead of a seperate API. mypkmndeck-api can be found [here](https://github.com/MrApproved/mypkmndeck_api).
